public class Stringer {
    // public String stringBits(String str) {
    //      String result = "";
    //     for (int i = 0; i < str.length(); i += 2) {
    //         result += str.substring(i, i + 1);
    //    }

    //    return result;
    // }

    //  public int last2(String str) {
    //      if (str.length() < 2) {
    //         return 0;
    //     }
    //         int count = 0;
    //        String verj = str.substring(str.length() - 2);
    //        for (int i = 0; i < str.length() - 2; i++) {
    //            String st = str.substring(i, i + 2);
    //            if (st.equals(verj)){
    //                 count++;
    //            }
    //       }
    //   return count;
    //}
    //  public int stringMatch(String a, String b) {
    //      int lenght = Math.min(a.length(),b.length());
    //      int count = 0;
    //    for (int i = 0; i < lenght-1;i++){
    //         String araj =a.substring(i,i+2);
    //        String erk =b.substring(i,i+2);
    //        if (araj.equals(erk)){
    //            count++;
    //       }
    //   }

    //  return count;
    //}
    //   public String altPairs(String str) {
    //    String st = "";
    //     for (int i =0; i <str.length();i+=4){
    //        int end = i + 2;
    //        if (end > str.length()) {
    //           end = str.length();
    //       }
    //       st += str.substring(i,end);
    // }
    //           return st;
    //}
    //  public int array667(int[] nums) {
    //    int count = 0;
    //    for (int i = 0; i < nums.length - 1; i++) {
    //       if (nums[i] == 6 && nums[i + 1] == 6 || nums[i + 1] == 7) {
    //            count++;
    //       }
    //    }
    //    return count;
    //}
    //  public boolean noTriples(int[] nums) {
    //    for (int i = 0; i < nums.length - 2; i++) {
    //       if (nums[i]==nums[i+1]&&nums[i]== nums[i+2]){
    //            return false;
    //        }
    //    }
//return true;
    // }

    // public boolean has271(int[] nums) {
    //    for (int i = 0; i < nums.length - 2; i++) {
    //       int gum = nums[i];
    //       if (nums[i + 1] == (gum + 5) && Math.abs(nums[i+2] - (gum -1)) <= 2 ){
    //           return true;
    //       }
    //   }
    //   return false;
    // }
    // int countXX(String str) {
    //   int count = 0;
    // for (int i = 0; i < str.length() - 1; i++) {
    //     if (str.substring(i, i + 2).equals("xx")) {
    //       count++;
    //  }
    //}
    //return count;
    //}


}